
using UnityEngine;

public class limitarRota : MonoBehaviour
{
    public GameObject limite;
    public float px;
    public float py;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
